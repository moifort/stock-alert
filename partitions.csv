# ESP32-S3 8MB partition table for HAP (single-image, no OTA for now).
# - `factory_nvs` is required by the HAP keystore even when we override the
#   setup code at runtime via hap_set_setup_code() — the SDK still opens the
#   partition during hap_init().
# Name,        Type, SubType,  Offset,   Size
nvs,           data, nvs,      0x9000,   0x6000
phy_init,      data, phy,      0xf000,   0x1000
factory,       app,  factory,  0x10000,  0x380000
factory_nvs,   data, nvs,      ,         0x6000
nvs_user,      data, nvs,      ,         0x6000
