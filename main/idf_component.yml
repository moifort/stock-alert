## Component manager manifest
## Doc: https://docs.espressif.com/projects/idf-component-manager/
##
## HAP components are not on the IDF Component Registry — they are pulled in
## via EXTRA_COMPONENT_DIRS pointing at $HOMEKIT_PATH (see root CMakeLists.txt).

dependencies:
  idf:
    version: ">=5.5,<6.0"
